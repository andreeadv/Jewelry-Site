<!DOCTYPE html>
<html  lang="ro">
    <head>
        <meta name="keywords" content="produse reduse,  bijuterii, accesoriu, cercei, colier, pret, bratara, comanda, favorite"><!--cuvintele cheie ale paginii; majoritatea motoarelor de căutare ignoră keywords -->
        <meta name="description" content="Site de Bijuterii.Coliere.Cercei.Bratari.Talismane.Pandantive.Aur.Argint.Reduceri."><!-- descrierea paginii, adesea apare sub link când sunt afișate rezultatele căutării -->
        <link rel="apple-touch-icon" sizes="180x180" href="./Resurse/ico/apple-touch-icon.png">

        <title>ChicBiju - home</title>
        <%- include("../fragmente/head") %>     
        <link href="/Resurse/css/produs.css" type="text/css" rel="stylesheet" />
    </head>
    <body>
    <%- include("../fragmente/header") %>

    <main>
        <section>
            <h2>Pagină produs</h2>
            <div class="produs">
                <article id="art-produs">
                    <h3><span id="titluprod" class="nume"><%- locals.prod.nume %></span></h3>
                    <p>Pret: <span class="pret"><%- locals.prod.pret %> </span></p>
                    <p>Descriere: <span class="descriere"><%- locals.prod.descriere %> </span></p>
                    <p>Gramaj: <span class="gramaj"><%- locals.prod.gramaj %> </span></p>
                    <p>Material: <span class="material"><%- locals.prod.material %> </span></p>
                    <p>Categorie: <span class="categorie"><%- locals.prod.categorie %> </span></p>
                    <% function yesorno(impachetare) {
                        if(impachetare)
                            return "DA";
                        else 
                            return "NU";
                    } %>
                    <p>Impachetatare cadou: <span class="impachetare"><%- yesorno(locals.prod.impachetare_cadou) %> </span></p>
                    <% function formatDate(date) {
                        const options = {
                            day: '2-digit',
                            month: 'long',
                            year: 'numeric',
                            weekday: 'long'
                        };
                        const formattedDate = new Date(date).toLocaleDateString('ro-RO', options);
                        //descompune șirul formattedDate în patru componente și le atribuie variabilelor respective weekday, month, day și year
                        const [weekday, day, month , year] = formattedDate.split(' ');
                        return `${weekday.charAt(0).toUpperCase() + weekday.slice(1)} ${day}-${month.charAt(0).toUpperCase() + month.slice(1)}-${year} `;
                    } %>
                    <p class="data_adaugare">Data Adaugare: <span class="val-data_adaugare"><%- formatDate(locals.prod.data_adaugare) %></span></p>

                    <figure class="imag-produs">
                        <img  src="/Resurse/imagini/produse/<%- locals.prod.imagine %>" alt="[imagine <%- locals.prod.nume %>]" />
                    </figure>    
                </article>
            </div>
        </section>
    </main>

    <%- include("../fragmente/footer") %>
    </body>
</html>